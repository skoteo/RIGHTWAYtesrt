# RIGHT WAY - Website Structure

## 📁 Структура файлов

```
├── index.html           # Главная страница
├── shop.html           # Страница каталога товаров
├── base-template.html  # Базовый шаблон для новых страниц
└── README.md          # Этот файл с инструкциями
```

## 🎨 Что есть в проекте

### 1. **index.html** - Главная страница
- Hero секция с большим заголовком
- Секция "About" с философией бренда
- Превью коллекции (6 товаров)
- Lookbook галерея
- Footer с соцсетями

### 2. **shop.html** - Полный каталог
- Страница со всеми товарами (18 товаров)
- Фильтры по категориям:
  - All (все)
  - Outerwear (верхняя одежда)
  - Tops (топы)
  - Bottoms (низ)
  - Accessories (аксессуары)
- Бейджи "New" на новых товарах

### 3. **base-template.html** - Шаблон
- Готовая структура для создания новых страниц
- Все стили и анимации уже подключены
- Комментарии с инструкциями

## ✨ Основные фичи

### Анимации
- **Reveal анимация**: элементы появляются снизу при скролле
- **Плавная навигация**: navbar прячется при скролле вниз
- **Hover эффекты**: на карточках товаров и ссылках
- **Shine эффект**: на плейсхолдерах изображений

### Навигация
- Автоматически скрывается при скролле вниз
- Появляется при скролле вверх или наведении
- Работает на всех страницах

## 🚀 Как создать новую страницу

1. Скопируй `base-template.html`
2. Переименуй файл (например, `about.html`)
3. Измени `<title>` в `<head>`
4. Замени содержимое в секции `<!-- Main Content Section -->`
5. Используй классы для анимаций:

```html
<!-- Элемент с анимацией появления -->
<div class="reveal">Контент</div>

<!-- Элементы с задержкой (для последовательного появления) -->
<div class="reveal">Элемент 1</div>
<div class="reveal" style="transition-delay: 0.1s;">Элемент 2</div>
<div class="reveal" style="transition-delay: 0.2s;">Элемент 3</div>
```

## 🎯 Полезные CSS классы

### Контейнеры
- `.container` - основной контейнер с padding
- `.page-header` - шапка страницы с заголовком

### Типографика
- `.page-title` - большой заголовок страницы
- `.section-title` - заголовок секции
- `.text-body-large` - крупный текст

### Кнопки
- `.btn` - основная кнопка с hover эффектом

### Изображения
```html
<!-- Плейсхолдер изображения с анимацией -->
<div class="img-placeholder" style="aspect-ratio: 16/9;"></div>

<!-- Можно менять пропорции -->
<div class="img-placeholder" style="aspect-ratio: 3/4;"></div>
<div class="img-placeholder" style="aspect-ratio: 1/1;"></div>
```

### Анимации
- `.reveal` - базовая анимация появления
- `.reveal.active` - активное состояние (добавляется автоматически)

## 🎨 Цветовая схема

```css
--color-bg: #d9d9d9;           /* Фон страницы */
--color-accent: #6B0000;       /* Акцентный красный */
--color-text: #1f1f1f;         /* Основной текст */
--color-text-light: #666666;   /* Светлый текст */
--color-placeholder: #000000;  /* Фон плейсхолдеров */
```

## 📝 Примеры использования

### Добавить секцию с контентом
```html
<section class="content-section">
    <div class="container">
        <h2 class="section-title reveal">Заголовок</h2>
        <p class="text-body-large reveal">Текст...</p>
    </div>
</section>
```

### Добавить сетку с карточками
```html
<div class="product-grid">
    <div class="product-card reveal">
        <div class="product-img img-placeholder"></div>
        <div class="product-info">
            <span class="product-name">Название</span>
            <span class="product-price">$180</span>
        </div>
    </div>
    <!-- Повторить для других карточек -->
</div>
```

## 🔗 Структура ссылок

Все ссылки работают между страницами:
- `index.html` → главная
- `shop.html` → каталог
- `#about` → секция About на главной
- `#lookbook` → секция Lookbook на главной
- `#footer` → футер

## 💡 Советы

1. **Всегда используй класс `.reveal`** для анимаций при скролле
2. **Добавляй `transition-delay`** для последовательного появления элементов
3. **Используй `.container`** для правильных отступов по краям
4. **Плейсхолдеры** имеют встроенную shine анимацию
5. **Навигация** работает автоматически на всех страницах

## 📱 Адаптивность

Сайт адаптивен для всех устройств:
- Desktop (1920px+)
- Tablet (900px-1920px)
- Mobile (<900px)

Grid автоматически подстраивается под размер экрана.

---

**Автор**: Claude  
**Дата**: 2025  
**Стиль**: Minimalist Streetwear
